<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mobilProje">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Uygulamalar"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Vizeodev">
        <activity
            android:name="com.example.mobilProje.navActivity"
            android:label="@string/title_activity_main6"></activity>
        <activity android:name="com.example.mobilProje.mainActivity5" />
        <activity android:name="com.example.mobilProje.exchangeActivity" />
        <activity android:name="com.example.mobilProje.calculatorActivity" />
        <activity android:name="com.example.mobilProje.appsActivity" />
        <activity android:name="com.example.mobilProje.loginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>